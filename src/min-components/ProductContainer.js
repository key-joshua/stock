import React from "react";
import BodyContainer from './BodyContainer';

const Product = (props) => {
    return (
        <div>
            <BodyContainer />
        </div>
    );
}

export default Product;